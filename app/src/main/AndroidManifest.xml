<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.ddns.zimportant.lovingpeople">

    <application
        android:name=".LovingPeopleApplication"
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppThemeNoActionBar">

        <activity android:name=".activity.SplashScreenActivity"
            android:theme="@style/SplashScreenTheme"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name">
        </activity>
        <activity android:name=".activity.WelcomeActivity" />
        <activity android:name=".activity.ConversationActivity"/>
        <activity android:name=".activity.IntroActivity"
            android:theme="@style/Theme.Intro" />
        <activity android:name=".activity.SearchCounselorsActivity"/>
        <activity android:name=".activity.RegisterCounselorActivity"/>
        <activity android:name=".activity.PrepareActivity"/>
        <activity android:name=".activity.RequestConversationActivity"/>
    </application>

</manifest>